<div class="container"style="margin-top: 6%">

  <div id="clients" class="form-control bg-light shadow-lg p-4">
    <div class="table-responsive scrollbar scrollbar-young-passion " style="max-height: 400px; ">
      <table class="table table-striped rounded table-bordered">
        <thead class="table-dark sticky-top">
          <tr>
            <th scope="col">ID</th>
            <th scope="col">Nombre</th>
            <th scope="col">Edad</th>
            <th scope="col">Pelicula</th>
            <th scope="col" colspan="3">Acciones</th> <!-- Cambio de título para las acciones -->
          </tr>
        </thead>
        <tbody>
          <% @clients.each do |client| %>
            <%= render client %>
          <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
